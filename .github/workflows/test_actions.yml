name: GitHub Test CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Respository variables
      run: echo $GITHUB_REPOSITORY
    - name: Git Branch
      run: echo $GITHUB_REF
    - name: Git SHA
      run: echo $GITHUB_SHA
